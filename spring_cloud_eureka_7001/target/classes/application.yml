server:
  port: 7001

# Eureka的配置
#spring:
#  application:
#    name: eurekaserver

eureka:
  instance:
    name: eureka7001.com
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka7002.com:7002/eureka/
